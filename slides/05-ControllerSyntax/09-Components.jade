section
    header
        h2 Controller Syntax
        h3 Components

    .break-md
        .two-column
            .col
                .small
                    div Inherits from Directive

                    .break
                        div +
                    .break
                        ul
                            li Creates Shadow DOM
                            li Paired with View annotation(s)
                            li Assigns Injector
                
            .col
                pre.
                    import {MyService} from '...';
                
                    <span class="highlight">@Component</span>({
        
                       // Same as @Directive
                       
                       <span class="highlight">appInjector:</span>
                           [ MyService ]

                       <span class="highlight">// and more ...</span>
                    })
